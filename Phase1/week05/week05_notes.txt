PANDAS

df = pd.read_csv("filename.csv")

print(df.head(number of rows to print)) -> top rows
df.tail() -> bottom rows

pd.read_excel(filename) -> reads excel

df = pd.read_csv("file.txt", delimiter="\t") -> tab separated txt data file; but it can be anything e.g. delimiter = "xxx"

print(df.columns) -> gives back the headers
print(df["name of the column"  #you can put more columns at once][0:5 #which cases])
print(df.iloc[1:4]) -> specific rows; ilock=integer lock
print(df.iloc[x,y]) -> specific cell

for index, row in df.iterrows():
	print(index, row ["name of the row"  #also possible])       -> prints each row

print(df.loc[df["name of the column"] == "Specific value"])